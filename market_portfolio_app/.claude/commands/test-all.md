# Test All

Run the project's complete verification suite.

Include:
- formatting/lint
- type checks if configured
- unit tests
- integration tests
- security tests
- coverage
- Docker build
- application health smoke test if the environment supports it

Fix failures caused by the implementation when safe. Never weaken or delete tests merely to obtain a pass. Summarize actual commands and results.
