# Docker Skill

Requirements:
- deterministic dependencies
- non-root runtime
- healthchecks
- no secrets in image
- environment-driven configuration
- PostgreSQL service for development
- optional reverse proxy
- clean shutdown

Do not use Docker host networking as a requirement.

Do not assume Linux-only behavior for the client.
