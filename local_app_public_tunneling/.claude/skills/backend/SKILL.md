# Backend Skill

Use:
- FastAPI
- Pydantic
- SQLAlchemy 2.x
- Alembic
- asyncio

Keep routes thin.

Use services for business logic.

Use dependency injection for auth and DB sessions.

Use typed interfaces for protocol/runtime components.

Return consistent errors.

Never expose stack traces to public clients.
