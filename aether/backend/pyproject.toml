[project]
name = "aether-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi","uvicorn[standard]","pydantic","sqlalchemy","asyncpg","alembic","redis","httpx"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
