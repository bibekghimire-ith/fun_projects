# Observability

Use structured logs, metrics and OpenTelemetry-compatible tracing.

Track request/model/tool latency, token usage, failures, approvals, automation failures,
queue depth, database latency and RAG latency.

Never log passwords, tokens, API keys or full private document contents.
