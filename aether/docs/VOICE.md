# Voice

Pipeline: microphone -> STT adapter -> agent -> TTS adapter -> speaker.

Support push-to-talk, optional continuous mode, local/offline STT/TTS and cloud adapters. Text
remains the fallback. Voice actions use the same permission model as text.
