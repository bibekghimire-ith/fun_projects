# Multi-user

Support multiple users even when the default deployment has one.

Every user-scoped resource must have an ownership boundary. Isolate conversations, memory,
RAG, integrations, secrets and audit attribution. Admins manage global configuration without
accessing private user content unless explicitly authorized.
