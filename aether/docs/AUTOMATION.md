# Automation

Triggers: schedule, webhook/event, manual, conversation command, system event.

An automation contains trigger, conditions, steps, approval gates, retry policy, timeout and
notification policy.

Persist execution state, use idempotency keys and never blindly retry unsafe external side effects.
