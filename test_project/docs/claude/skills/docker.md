# Skill: Docker Portability

Every service must run without host-specific assumptions.

Requirements:
- Dockerfiles
- Compose
- health checks
- environment variables
- persistent volumes
- no hard-coded host paths
- Linux/macOS/Windows-compatible commands where practical

Local default:
PostgreSQL + MinIO + API + Web.
