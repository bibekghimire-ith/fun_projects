# Copy to docker-compose.override.yml for local dev (live reload, source mount):
services:
  web:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./app:/app/app
